const SettingsButton = ({ onClick }) => (
  <button
    type="button"
    aria-label="Settings"
    className="icon-btn"
    onClick={onClick}
  >
    ⚙
  </button>
)

export default SettingsButton
