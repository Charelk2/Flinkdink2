import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.flinkdink.app',
  appName: 'FlinkDink',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
